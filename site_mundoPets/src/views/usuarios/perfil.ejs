<!DOCTYPE html>
<html lang="es">
    <%- include('../partials/head',{title: locals.usuarioLogin.nombre}) %> 
<body class="perfil">
    <%- include('../partials/header') %>
    <main>
        <div class="imgPerfil">
            <img src="/img/usuarios/<%= locals.usuarioLogin.imagen %>  " alt="">
        </div>
       
        <h1>Hola <%= `${locals.usuarioLogin.rol == 1 ? 'Administrador' : 'Usuario' } ${locals.usuarioLogin.nombre}` %></h1>
        
        <form action="/usuarios/cerrarSesion?_method=DELETE" method="post">
            <button type="submit">Cerrar Sesion</button>
        </form>
    </main>
    
</body>
</html>